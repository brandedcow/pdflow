# Backend (FastAPI) — copy relevant values into .env.local at the project root
GROQ_API_KEY=your_groq_api_key_here

# Mobile app (Expo) — copy relevant values into .env at the project root
# Required for production builds. Leave unset in dev to use automatic device host detection.
EXPO_PUBLIC_BACKEND_URL=https://your-backend-url.com
